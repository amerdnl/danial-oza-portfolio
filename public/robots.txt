User-agent: *
Allow: /

# No Sitemap directive yet: it would need the live domain, and pointing at a
# sitemap that does not exist is worse than omitting the line. Add a
# sitemap.xml and reference it here once the site has its real domain.
